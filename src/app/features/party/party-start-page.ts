import { Component } from '@angular/core';

/** Milestone 5: constraints (runtime, movie/show/either, streamable-by-all), join code generation. */
@Component({
  selector: 'pp-party-start-page',
  template: `
    <div class="mx-auto flex min-h-dvh max-w-md flex-col items-center justify-center gap-3 px-6 text-center">
      <div class="text-4xl">🎉</div>
      <h1 class="font-display text-2xl font-semibold">Start a party</h1>
      <p class="text-sm text-muted-2">Coming in milestone 5 — constraints, join codes, and the live lobby.</p>
    </div>
  `,
})
export class PartyStartPage {}
