// Placeholder. After the database is set up (milestone 2), regenerate with:
//
//   supabase gen types typescript --linked > src/app/core/types/database.types.ts
//
// and type the client in core/supabase.client.ts with `createClient<Database>(...)`.
export type Database = Record<string, never>;
